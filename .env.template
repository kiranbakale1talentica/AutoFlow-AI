# AutoFlow AI Environment Configuration Template
# Copy this file to .env and update the values

# Backend Configuration
NODE_ENV=production
PORT=5000
CORS_ORIGIN=http://localhost:3000

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# GitHub Integration (Optional)
# Get these from: https://github.com/settings/applications/new
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret



# Email Configuration (Required for OTP)
# For Gmail: use smtp.gmail.com with app password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=autoflow@ai.com

# Backup Configuration (Optional)
BACKUP_SCHEDULE=0 2 * * *

# Development URL for webhooks (Optional)
# Use ngrok or similar for local development
NGROK_URL=https://your-ngrok-url.ngrok.io

# Frontend Configuration (Optional - defaults are usually fine)
REACT_APP_API_URL=http://localhost:5000
REACT_APP_WS_URL=ws://localhost:5000
